services:
  - type: web
    name: unlock-backend
    runtime: java
    rootDir: backend
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/unlock-backend-0.0.1-SNAPSHOT.jar
    plan: free